import CustomButton from '../../Customs/CustomButton'

const HangoutNavBar = () => {
  return (
    <> 
        {/* sam use a placeholder image here for now i'll replace it when i pass the logic for this bit */}
        <div id="header-bar" style={{backgroundImage}}>
            
        </div>
    </>
  )
}

export default HangoutNavBar